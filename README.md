# Form constructor Yii2
it allows you to easily create as many forms as you want with different fields

## Usage
```
echo \frontend\widgets\FormWidget::widget([
    'form' => $form_id,
    'form_title' => 'name of your form'
]);
```
